#include <stdio.h>
#include <unistd.h>

int main() {
    pid_t parent_pid = getppid();
    pid_t current_pid = getpid();

    printf("Parent PID: %d\n", parent_pid);
    printf("Current PID: %d\n", current_pid);

    return 0;
}
